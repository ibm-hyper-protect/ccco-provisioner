type: env
logging:
  logRouter:
    hostname: "{{ log_router_host }}"
    iamApiKey: "{{ log_router_key }}"
    port: 443
host-attestation:
  "{{ hkd_name }}":
    description: KEY-1
    host-key-doc: "{{ hkd_cert.content | trim }}"
signingKey: "{{ signing_key.content | b64decode | trim }}"
